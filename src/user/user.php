<?php
    //Connexion a la db
    $host = "";

    //Récupération des information de l'utilisateur
    $id = $_POST['id_utilisateur'];

    //Récupération des informations des tickets de l'utilisateur
    if (isset($_GET['id']) == $id) {
        foreach($data as $utilisateur) {
            echo "";
        };
    };
?>